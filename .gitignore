node_modules

.env
.env.local
.DS_Store
.cache
.output
.nitro
.tanstack

/dist/
/dist-ssr/

# Vercel Build Output API bundle (produced by build-vercel.sh at go-live).
/.vercel/

# Runtime server log written on start.
/.run/

# Generated by the TanStack Start router plugin on dev/build.
src/routeTree.gen.ts

# Local secrets — never commit
.env

# Static Netlify export (generated) — regenerable, never commit.
dist-netlify/
# Netlify deploy bundle produced by the deploy flow (zips the export, incl. the
# generated reg-lookup function with the provider key inlined at export time).
# Generated artifact + carries a secret — NEVER commit.
dist-netlify.zip
